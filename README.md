# Recruitment-Agency-Workflow
I used LangGraph to build the workflow logic, where each step like experience classification and skill checking is handled as nodes, and conditional edges decide the final outcome. I used prompt inside those nodes to ensure consistent matching outputs. I used a state object to pass data like experience level and skill match between steps. 
